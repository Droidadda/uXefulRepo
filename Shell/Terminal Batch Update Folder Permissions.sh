#Navigate to the directory that contains the folders to change permissions for.
cd ~/Desktop/Work\ In\ Progress/

#Change permissions recursively for all folders in this directory.
sudo chmod -R 707 *
